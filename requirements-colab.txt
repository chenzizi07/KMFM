numpy>=1.24,<3
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
tabulate>=0.9
pytest>=7.4
# Colab already provides a CUDA-enabled PyTorch. Do not reinstall torch unless
# the runtime reports that it is missing.
